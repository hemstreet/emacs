;; variables.el --- set all of our variables we need -*- lexical-binding: t; -*-

;; `user-emacs-dir` loaded in init. It's the root of emacs
;; `core-dir` was already loaded. Root of the core lib

(setq backup-directory "~/.emacs.backups")

(provide 'variables)
